// Conjunt de constants globals de l'aplicació.
// createdAt: 24/07/18 dj. JIQ

import 'package:flutter/material.dart';

// Internacionalització ---------------
const esCode = "es";
const localeES = Locale(esCode);
const caCode = "ca";
const localeCA = Locale(caCode);
const enCode = "en";
const localeEN = Locale(enCode);
const frCode = "fr";
const localeFR = Locale(frCode);
const ptCode = 'pt';
const localePT = Locale(ptCode);

const String unknownTextKey = "#UNKNOWN_TEXT_KEY";

// Magatzem segur de dades ------------
const ssUserId = "user_id";
const ssUserLang = "user_lang";
const ssFbToken = "fb_token";
const ssFbTokenNew = "fb_tokenNew";
const ssDbPass = "db_pass";
const ssDbVersion = "db_version";
const ssPassBase = "pass_base";
const ssRandomSeq = "random_seq";
const ssMustAuthenticate = "must_authenticate";

const ssCounter = "counter";

// Tamanys de disseny de pantalles ----
const Size iPhone8PlusSize = Size(414.0, 736.0);
